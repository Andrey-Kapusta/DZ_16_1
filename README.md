# Работа с массивами в C

## 📋 Задания к лаб. 11

1. Заполнение массива с консоли
2. Преобразование массива
3. Печать массива
4. Найти произведение всех элементов массива между первым и вторым 0

## 📋 Задания к лаб. 14

1. Заполнение массива значениями из функции Func(x)
2.  Печать массива
3.  Поиск в массиве первого отрицательного значения
4.  Поиск в массиве первого нулевого значения
5.  Поиск в массиве первого положительного значения

## 📋 Задания к лаб. 16

1. Обработка элементов массива
2. Печать массива
3. Удаление элемента массива
4. Вставка `-999` в рандомное место массива
5. Выполнение домашнего задания (генерация массива на основе других массивов)

## 📁 Структура проекта

├── .gitattributes

├── .gitignore

├── app.c

├── array.h

├── arrays.c

├── DZ_16_1.sln

├── DZ_16_1.vcxproj

├── DZ_16_1.vcxproj.filters

└── README.md


## 🔧 Реализованные функции

### 1. `double func(double x)`
- **Назначение**: Получение уникального вычисленного значения
- **Параметры**:
  - `x` — изменяемый параметр
- **Возвращает**: вычисленное уникальное значение



### 2. 'int find_first_element(double* array, int size, int type)'
- **Назначение**: Поиск первого элемента в массиве
- **Параметры**:
  - 'ptr_array' - указатель на массив
  - 'size' - размер массива
  - 'type' - тип элемента
- **Возвращает**: индекс найденного элемента или -1 если не найден
 




### 3. 'double* full_elements(double* ptr_array, int n)'
- **Назначение**: Заполнение массива значениями
- **Параметры**:
  - 'ptr_array' - указатель на массив
  - 'n' - размер массива
- **Возвращает**: указатель на заполненный массив





### 4. 'double* full_elements_r(double* ptr_array, int n)'
- **Назначение**: Заполнение массива значениями
- **Параметры**:
  - 'ptr_array' - указатель на массив
  - 'n' - размер массива
- **Возвращает**: указатель на заполненный массив
 




### 5. 'double* full_elements4(double* ptr_array4, double* ptr_array1, int size4);'
- **Назначение**: Заполнение массива значениями
- **Параметры**:
  - 'ptr_array4' - указатель на заполняемый массив 
  - 'ptr_array1' - указатель на с исходными данными массив
  - 'n' - размер массива
- **Возвращает**: указатель на заполненный массив







### 6. 'int put_elements(double* ptr_array, int size)'
- **Назначение**: Печать элементов массива
- **Параметры**:
  - 'ptr_array' - указатель на массив
  - 'n' - размер массива
- **Возвращает**: количество выведенных элементов










### 7. 'double* calc_elements(double* ptr_array, int size)'
- **Назначение**: Обработка элементов массива согласно индивидуальному варианту
- **Параметры**:
  - 'ptr_array' - указатель на массив
  - 'size' - размер массива
- **Возвращает**: значения после обработки массива








### 8. 'int delete_k(double* ptr_arr, int size, int k);'
- **Назначение**: Удаление элемента из массива
- **Параметры**:
  - 'ptr_array' - указатель на массив
  - 'size' - размер массива
  - 'k' - индекс удаляемого элемента
- **Возвращает**: размер нового массива







### 9. 'int max_elem(double* ptr_array, int size)'
- **Назначение**: Поиск максимального элемента в массиве
- **Параметры**:
  - 'ptr_array' - указатель на массив
  - 'size' - размер массива
- **Возвращает**: индекс найденного элемента







### 10. 'int last_negative(double* ptr_array, int size)'
- **Назначение**: Поиск последнего отрицательного элемента в массиве
- **Параметры**:
  - 'ptr_array' - указатель на массив
  - 'size' - размер массива
- **Возвращает**: индекс найденного элемента









### 11. 'double* insert(double* ptr_arr, int* size, int index, double num)'
- **Назначение**: Вставляет число num по указанному индексу в динамический массив.
- **Параметры**:
  - 'ptr_arr' - Указатель на массив типа double
  - 'size' - Указатель на переменную, хранящую размер массива
  - 'index' - Индекс для вставки (0 ? index ? *size)
  - 'num' - Число для вставки
- **Возвращает**: Указатель на обработанный массив. В случае ошибки возвращает NULL и освобождает память.


